name: CI

# Build + lint + the full Jest suite. Deliberately Docker-free: the test suite
# stubs Redis/Mongo/SQS, so it proves the logic layer on a clean runner with no
# infra. The live demo (npm run infra:up) is the only thing that needs Docker.
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version: [20.x, 22.x]
    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build (strict TS)
        run: npm run build

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm test
